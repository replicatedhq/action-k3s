name: k3s Testing
on: pull_request
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: ./action.yml
      id: k3s
      with:
        version: 'latest'
#    - uses: replicatedhq/actions-k3s@v1.0.4
#      id: k3s
#      with:
#        version: 'latest'
    - run: |
        kubectl get nodes